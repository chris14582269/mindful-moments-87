[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["app"]
omit = ["app/db/models.py", "app/db/session.py", "alembic/*"]

[tool.coverage.report]
fail_under = 80
show_missing = true
